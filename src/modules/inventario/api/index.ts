// Barrel: un archivo `<recurso>.api.ts` por controller del backend, con
// funciones puras (sin React/SWR) que llaman al httpClient compartido.
export * from './categorias.api'
export * from './productos.api'
